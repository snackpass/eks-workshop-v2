# https://taskfile.dev

version: "3"

vars:
  KUBECTL: kubectl --context {{ .KUBE_CONTEXT }}

tasks:
  diff:k:
    cmds:
      - |
        {{.KUBECTL}} diff -k {{.path | default "."}} {{.CLI_ARGS}}
    ignore_error: true
